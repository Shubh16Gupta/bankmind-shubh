# BankMind API – Track C (System Builder)

This is a FastAPI service that predicts whether a bank customer will subscribe to a term deposit, and provides plain‑English explanations using Groq's LLM.

## Setup (macOS)

1. **Clone / navigate** to the project folder.
2. **Create virtual environment**:
   ```bash
   python3 -m venv venv
   source venv/bin/activate